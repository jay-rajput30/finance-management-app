import styles from "./Dashboard.module.css";
const Dashboard = () => {
  return (
    <div className={styles.dashboardContainer}>
      <section className={styles.incomeExpenseFormContainer}></section>
    </div>
  );
};

export default Dashboard;
